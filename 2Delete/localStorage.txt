//--------------------------------------------------------------------
localStorage.setItem(key,value); to set
localStorage.getItem(key); to get
//--------------------------------------------------------------------

- localStorage.setItem("city", "Noida"): This method is used to add the data through key and value to localStorage.
- const res = localStorage.getItem("city"): It is used to fetch or retrieve the value from the storage using the key.
- localStorage.removeItem("city"): It removes an item from storage by using the key.
- localStorage.clear(): It is used to clear all the storage.

```js
// Storing and retrieving nested objects in LocalStorage
let nestedObject = { key1: 'value1', key2: { nestedKey: 'nestedValue' } };
localStorage.setItem('nestedObject', JSON.stringify(nestedObject));

let retrievedNestedObject = JSON.parse(localStorage.getItem('nestedObject'));
```

JavaScript code to check browser compatibility: 
```js
<html>  
<body>  
  
<div id="result"></div>  
  
<script>  
// Check browser support  
if (typeof(Storage) !== "undefined") {  
  // Store an item to localStorage  
  localStorage.setItem("firstname", "Alen");  
    
  // Retrieve the added item  
  document.getElementById("result").innerHTML = localStorage.getItem("firstname");  
    
} else {  
  //display this message if browser does not support localStorage  
  document.getElementById("result").innerHTML = "Sorry, your browser does not support Web Storage.";  
}  
</script>  
</body>  
</html>  
```

//--------------------------------------------------------------------
shoppingCart
productList.html

ShoppingController
//--------------------------------------------------------------------
 @GetMapping("/shoppingCart/checkout")
 shoppingCartService.checkout();
 
 
//--------------------------------------------------------------------
https://github.com/abbassizied/shopping-cart/blob/main/cart.html.txt

//--------------------------------------------------------------------
      <button onclick="add()">Add Item <i class="fa-solid fa-plus"></i></button>
      <button onclick="del()">Clear all <i class="fa-solid fa-ban"></i></button>
//--------------------------------------------------------------------

//--------------------------------------------------------------------

//--------------------------------------------------------------------


//--------------------------------------------------------------------


//--------------------------------------------------------------------


//--------------------------------------------------------------------


//--------------------------------------------------------------------


//--------------------------------------------------------------------


//--------------------------------------------------------------------